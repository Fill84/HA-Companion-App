# Verificatiestatus van de projectherstelwerkzaamheden

Bijgewerkt: 24 september 2026. Dit is de uitvoeringsstatus bij de [specificatie](../specs/2026-09-24-project-recovery.md), geen releaseverklaring. `Gedeeltelijk` betekent dat code bestaat maar ten minste één acceptatieproef uit de audit nog ontbreekt. Geen van de 54 auditpunten krijgt op basis van alleen een unit-test de status volledig afgerond.

Nieuwe architectuureis: de gebruiker heeft de extern geïnstalleerde PawnIO-/LibreHardwareMonitor-helperroute vervangen door [volledig geïntegreerde sensortoegang](../specs/2026-09-24-integrated-sensors.md). De broncode en geïnstalleerde Windows-app gebruiken de eigen Rust-service via lokale IPC; de NSIS-build bevat eigen service en gesigneerde driverartefacten. Op `phill-pc` is één volledige app-naar-HA-CPU-temperatuurmeting bevestigd. De twee-host-GUI-proef, AMD en Linux/macOS-runtimeproeven blijven open releasepoorten. Oudere Windows-providervermeldingen hieronder zijn historische proefresultaten, geen bewijs voor de nieuwe route.

### Nieuwe geïntegreerde-providerproef op beast-unit

Op `beast-unit` (Windows 10 x64, Intel Core i7-10700K) was PawnIO 2.2.0 al in de DriverStore aanwezig, maar er was geen `Root\\PawnIO`-apparaat. De INF, CAT en SYS van die bestaande installatie hebben exact dezelfde SHA-256-hashes als onze vastgepinde, ondertekende bundel. Een gestarte kernelservice zonder PnP-apparaat kon niet worden geopend. De eigen installer maakt in dit geval nu het ontbrekende rootapparaat aan en koppelt de meegeleverde, geverifieerde INF. Een stille NSIS-installatie installeerde de app zonder optionele driverkeuze; daarna slaagde de expliciete `--install-with-driver`-stap. Windows meldde `ROOT\\PAWNIO\\0000` als `OK`, beide services draaiden en de eigen directe driverproef eindigde met code 0. De read-only named pipe gaf `protocol=1`, `status=0` en `celsius=51` terug. Een ongeldig pipe-verzoek leverde nul antwoordbytes; de eerstvolgende geldige aanvraag gaf weer een temperatuur. Een reeks van 100 losse geldige aanvragen leverde 100 geldige snapshots op in 146 ms wandtijd en 0,016 s CPU-tijd van de service; dit is een korte microproef, geen langdurige impactmeting. Verwijderen en opnieuw installeren van onze sensorservice slaagde beide keren met code 0; de gedeelde PawnIO-driver bleef daarbij aanwezig. Een echte stille NSIS-upgrade en -uninstall/-herinstall slaagden na correctie van de servicepaden in de hooks. De eerste upgradeproef legde een onveilige map-ACL bloot: de installer gaf de ingelogde gebruiker schrijfrechten op de LocalSystem-servicebinary. De installer zet nu de ACL van de applicatieboom terug, sluit de overerving op de installatiemap, geeft alleen SYSTEM en Administrators schrijfrechten en wijzigt het eigenaarschap naar Administrators. De laatste upgradeproef eindigde met code 0, een draaiende service en een servicebinary met alleen SYSTEM/Administrators `FA` en Users `RX`; na een volledige uninstall/reinstall gaf de pipe opnieuw `status=0`, `celsius=42`. Dit bewijst een echte Intel-meting en de geïnstalleerde servicelifecycle op één computer. De GUI-/Home-Assistant-route, tweede Windows-host, AMD en macOS/Linux-runtimeproeven blijven open; er is niet gepusht of gereleased.

Op `phill-pc` is daarna de actuele NSIS-installer verhoogd uitgevoerd, gevolgd door de meegeleverde `--install-with-driver`-stap; beide eindigden met code 0. Vooraf waren PawnIO en onze sensorservice op deze host afwezig. Daarna stonden beide services op `Running`, meldde Windows het PawnIO-apparaat als `OK` en gaf de lokale pipe `protocol=1`, `status=0`, `celsius=66`. De geïnstalleerde GUI draait uit `C:\Program Files\Home Assistant Companion\ha-companion.exe` en de installatiemap heeft de beperkte ACL. De bestaande lokale instelling `cpu_temperature_provider` stond nog op `false`; daarom bleef de eerste dashboardkaart terecht op `NaN` ondanks de werkende service. Na expliciete inschakeling en herstart van de GUI meldde de Home Assistant REST-state van de **bestaande** entity `sensor.phill_pc_cpu_temperature` `61.0` met `provider_status=ok` en `measurement_source=pawnio/2.2/intel-msr`. De gebruiker bevestigde daarna visueel een kaart met `52.0 °C`. Dit is de eerste end-to-end-proef op `phill-pc`; een enkele succesvolle proef bewijst nog geen lange-termijnstabiliteit of alle sensorcategorieën. `beast-unit` toonde in HA `Disconnected` omdat daar alleen de installer/service werd getest en geen aangemelde interactieve desktopapp is gestart. Er is niet gepusht of gereleased.

Legacy-inventaris op `phill-pc`: `C:\Users\philp\AppData\Local\com.ha-companion.desktop\WinRing0x64.sys` en `C:\Windows\System32\Drivers\WinRing0x64.sys` hebben dezelfde SHA-256 `11bd2c9f9e2397c9a16e0990e4ed2cf0679498fe0fd418a3dfdac60b5c160ee5`. De Windows-service `WinRing0x64` is `Stopped` en `Disabled`; de nieuwe app gebruikt hem niet. Een poging om alleen het appdata-exemplaar te verwijderen werd door de automatische toolcontrole met `blocked by policy` geweigerd. Beide resten zijn daarom onaangeroerd; aan de globale driver is ook geen eigendom van deze app bewezen.

| Status | Audit-ID's | Nog nodig voor afronding |
|---|---|---|
| Gedeeltelijk | SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, SEC-08, SEC-09, SEC-10 | Echte webview-/TLS-/HA-permissietests, legacy-driverinventaris en redactiecontrole. `get_settings` en registratiecommands leveren geen volledig token of webhook-ID meer. Opslagmigratie naar de OS-kluis is geïmplementeerd. De Tauri-capability beperkt plugin- en eigen IPC-commands nu tot de lokale `main`-webview; een statische contracttest en `cargo check` slagen, een echte afwijzingsproef vanuit de externe HA-webview ontbreekt nog. De wegwerpcredentialproef slaagt in interactieve Windows-gebruikerscontext op beide hosts; de eerdere SSH-fout `NoStorageAccess(Error(1312))` betrof de niet-interactieve sessie. Linux Secret Service en macOS Keychain zijn nog niet in een runtimeproef beoordeeld. De integratie laat een legacy-entry zonder eigenaar nu alleen door een HA-beheerder claimen; beide live apparaatentries hebben na gecontroleerde migratie een eigenaar. Het ongebruikte `/api/desktop_app/update`-eventendpoint is live verwijderd. Of normale HA-gebruikers nieuwe eigen apparaten mogen aanmelden, is nog een afzonderlijke beleidskeuze. |
| Gedeeltelijk | REL-01, REL-02, REL-03, REL-04, REL-05, REL-06, REL-07, REL-08 | End-to-end storingsmatrix, automatische reconnect na herstart, OS-shutdown en echte diskfout. Een onleesbaar instellingenbestand of ongeldige sensor-ID-map wordt nu expliciet gemeld; registratie en opslaan blijven dan geblokkeerd zodat geen nieuwe device-ID de bestaande identiteit vervangt. Een opgeslagen update-interval buiten 5–3600 seconden wordt expliciet afgewezen in plaats van een drukke lus te starten. De settings-store gebruikt geen achtergrondschrijver meer en vervangt het JSON-bestand via een gesynchroniseerd tijdelijk bestand en rename; een fout herstelt de vorige in-memory waarden. Op `beast-unit` is de autostart-Run-vermelding bij een tijdelijk uitgeschakelde instelling verdwenen en na herstel teruggekeerd; echte af-/aanmelding en verplaatst executable zijn nog niet beproefd. De actuele Windows-build bevat ook een afsluitvlag die nieuwe achtergrondupdates en herstelregistratie stopt vóór een offline-melding; een al lopende HTTP-call en de twee-secondenlimiet blijven in de echte OS-afsluitproef te beoordelen. |
| Gedeeltelijk | HA-01, HA-02, HA-03, HA-04, HA-05, HA-06, HA-07 | Lokale tests toetsen het webhookcontract, metadata, ontbrekende binaire waarden, rollback bij mislukte platformstart en nu ook opslagfouten tijdens sensorregistratie en verwijdering. Gelijktijdige registraties worden geserialiseerd. De correctie draait op `render-unit`; na herstart bleven 99 entity-ID's gelijk en beide Windows-clients leverden verse CPU-waarden. Een echte HA-proef voor geforceerde opslagfout, timer/dispatcher/entities/unload, repair en oudere clientpayloads ontbreekt nog. |
| Gedeeltelijk | SEN-01, SEN-02, SEN-05, SEN-06 | Fysieke hostmatrix, semantische meetproef en HA-statistiekcontrole. Diverse numerieke meetwaarden en swap-attributen worden nu als JSON-getal in plaats van geformatteerde string gestuurd. Een negatieve WMI-I4-waarde wordt nu afgewezen en is getest; recorder-/statistiekmigratie blijft open. |
| Gedeeltelijk | UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07 | UI-toegankelijkheidsproef, staged sensorvoorkeuren, fout- en herstelflows; sensorcategorieën en tray zijn vertaald. Setup, opslaan en opnieuw verbinden tonen nu gelokaliseerde foutmeldingen zonder ruwe backendtekst; andere backendpaden en native 200%-zoom/schermlezerproef blijven open. Dialoogrol, focusbeheer, live meldingen, scrollbare setup en knopcontrast zijn aangepast. Animatie pauzeert buiten de zichtbare setup, bij achtergrondvensters en bij reduced motion; native visuele proef ontbreekt. |
| Gedeeltelijk | DEP-01, DEP-02, BUILD-01, BUILD-02, BUILD-03, TEST-01, PERF-01, PERF-02, OBS-01 | Platformscan, echte CI-run, installerproef en hardwareprofiel; JS-scan meldt nul advisories. Zowel periodieke als initiële hardwarecollectie en initiële apparaatmetadata draaien nu op de blocking pool. De desktopbron registreert bij een volledig snapshot alleen nieuwe of gewijzigde sensormetadata; iedere tiende cyclus blijft een volledige waardenupdate sturen en HTTP 409 wist de cache voor herstel. Een echte CPU-/I/O-/HA-requestprofielmeting van deze nieuwe build ontbreekt nog. |
| Gedeeltelijk | ARCH-01 | Het wire-protocol heeft één byte-identieke fixture in beide zelfstandige repositories. De 23 selecteerbare sensoren gebruiken één Rust-catalogus voor ID, Engelse/Nederlandse keuze-naam en intervalstatus; de frontend ontvangt deze labels van de backend. De tweede in-memory registratieboolean is verwijderd: de duurzaam bewaarde webhook-ID is nu de enige lokale registratiebron. Dynamische Home Assistant-metadatadefinities en een expliciete toestand voor bereikbaarheid/aflevering zijn nog niet geconsolideerd. |
| Open | BUILD-06 | De macOS-/Linux-runtimeproeven van de platformmatrix zijn op verzoek uitgesteld; de implementatie en buildconfiguratie voor beide platformen blijven onderdeel van het project. |
| Gedeeltelijk | SEN-03 | GPU's gebruiken waar beschikbaar NVML-UUID of WMI-PNP-ID, batterijen serienummer en displays WMI-PNP-ID; de lokale suffixkoppeling wordt bewaard. Voor netwerk gebruikt de 1.0.5-bron op Windows de adapter-GUID via IP Helper en elders een uniek geldig MAC-adres; voor schijven gebruikt hij een Windows-volume-GUID, Linux-filesystem-UUID of macOS-persistente volume-UUID. Een oude suffix wordt alleen met een geverifieerde migratiekoppeling hergebruikt. Dubbele/ontbrekende identiteiten worden niet aan een andere meting toegewezen, zodat oude entities `unknown` worden. Beide Windows-hosts hebben de actuele installer ontvangen; op `beast-unit` kwamen twee nieuwe Ethernet-entities bij terwijl alle 99 oude ID's bleven bestaan. Gecontroleerde hotplug-/reboottests op beide hosts blijven open. |
| Gedeeltelijk | SEN-04 | Volledige/dynamische snapshots maken ontbrekende waarden `unknown` zonder entity-ID te wijzigen; oudere clients behouden hun gedrag. Voorkeurwijzigingen starten direct een volledige registratie en update. Echte hotplug-, uitschakel- en herstelproeven met HA ontbreken nog. |
| Gedeeltelijk | BUILD-05, DOC-01, DOC-02 | Handmatig releasepad met vaste integratie-SHA, versiepoort en bron-/hashmanifest; documentatie aangepast. De tsParticles-bundel is byte-identiek aan het officiële npm-pakket 3.2.0 en de notices worden gekopieerd. Nog geen daadwerkelijke release, signing/notarisatie, HA-versiematrix, volledige SBOM of clean-installproef. |
| Gedeeltelijk | BUILD-04, BUILD-07 | Aparte PR/push-CI met Windows/macOS/Linux, format, Clippy, Rust- en frontendtests toegevoegd; release is nu handmatig. Lokale Windows-controles slagen, maar hosted runners en echte HA-compatibiliteitsmatrix zijn nog niet uitgevoerd. HACS-minimum is conservatief op 2026.9.0 gezet. |

Aanvullende wijzigingen van 24 september, nog zonder geïnstalleerde eindproef: bij wijziging van de optionele CPU-temperatuurprovider voert de desktopapp direct een volledige sensorsynchronisatie uit. Bij ontbrekende registratie probeert de achtergrondlus, mits server en token zijn ingesteld, hoogstens eenmaal per minuut opnieuw te registreren; een gedeeld slot voorkomt dubbele handmatige/automatische registratie. Webhookverzoeken krijgen protocolversie 1; de app accepteert oudere HA-antwoorden zonder versie maar weigert een expliciet onbekende versie. De HA-integratie valideert metadata, sensorwaarden en protocolversie vóór mutatie van sensoropslag of heartbeat. De lokale controles na deze wijziging: Rust **24 geslaagd, 1 overgeslagen**, Clippy en format geslaagd; frontend **4/4**; HA-integratie **29/29**. Deze tests vervangen geen geïnstalleerde herstelproef of fysieke hostmatrix.

De Linux-GPU-collector gebruikt nu read-only DRM/sysfs in plaats van `rocm-smi`, en ontdekt AMD-/Intel-adapters ook naast een NVML-NVIDIA-adapter. De waarden zijn optioneel en blijven leeg als de kernel ze niet publiceert. Deze platformwijziging is nog niet op Linux gebouwd of uitgevoerd; de eerder genoemde Windows-Rusttests compileren dit `cfg(target_os = "linux")`-pad niet.

De macOS-GPU-parser leest nu de eenheid uit de `system_profiler`-VRAM-tekst voordat hij naar de bestaande MB-sensor omrekent; een onbekende eenheid levert geen VRAM-waarde. De eenheidstests hiervoor slagen op Windows, maar de `system_profiler`-route zelf is nog niet op macOS uitgevoerd.

Een registratie wordt nu alleen lokaal ongeldig verklaard bij een expliciet geparseerde webhook-HTTP-status 404 of 410. Een fouttekst met dezelfde cijfers elders telt niet als verloren registratie; de gerichte Rust-regressietest hiervoor slaagt. De laatste lokale controles: Rust **26 geslaagd, 1 overgeslagen**, Clippy en format geslaagd; frontend **4/4** en HA-integratie **29/29**. Dit is nog niet in een geïnstalleerde netwerkstoringsproef doorlopen.

De matrix is bewust streng: vooral de afzonderlijke HA-repo en echte Windows/macOS/Linux-hosts zijn nog niet samen in een releaseproef gebruikt. De bestaande lokale wijziging aan `ha-integration/.gitignore` is van de gebruiker en wordt niet als herstelwerk geteld.

## Vervolgcontrole van installatie, integratie en HACS

De live dashboardrijen voor uptime op `render-unit` lezen nu het bestaande `human_readable`-attribuut van dezelfde entities. De gebruiker bevestigde `19h 39m` voor `phill-pc`; de numerieke `system_uptime`-state blijft voor automations en statistieken behouden. `beast-unit` is nog offline, zodat de tweede rij pas na een nieuwe verbinding visueel kan worden bevestigd.

De eerdere lokale Windows NSIS-proefbuild was **1.0.8**; de hash en installatieproeven staan in de [vervolgaudit](../audit/2026-09-24-installation-ha-hacs-follow-up.md). De driver en de eigen sensorservice zijn standaard onderdeel van de Windows-installatie; de tweede appschakelaar is verwijderd. De installer is niet Authenticode-ondertekend en is niet als publieke, geteste release aangemerkt. HA op `render-unit` draait kandidaatversie **1.0.11**, één patch na de publieke integratierelease 1.0.10. HACS kan die pas aanbieden nadat de integratierepository volgens de [HACS-releaseprocedure](https://github.com/Fill84/ha-integration/blob/main/docs/RELEASE.md) apart is gepubliceerd. Er is niet gepusht of gereleased.

De broncode staat inmiddels op **1.0.5**, precies één patch na de publiek beschikbare desktoprelease 1.0.4. De tussenliggende 1.0.8- en 1.0.9-versies waren uitsluitend lokale proefbuilds en krijgen geen release of tag. Op `phill-pc` gaven twee adapters hetzelfde MAC-adres terug; de Windows-adapter-GUID uit `GetAdaptersAddresses` onderscheidt ze wel. De lokale optionele OS-proeven bevestigen dat de collector een GUID en een Windows-volume-ID leest. Bestaande HA-unique-ID's, MAC-adressen, volumegroottes en actuele OS-GUID's zijn op beide pc's geïnventariseerd en 11 respectievelijk 6 geverifieerde suffixkoppelingen zijn met instellingenback-up opgeslagen. De lokale **1.0.5**-installer (SHA-256 `B8E4ACBE441782CCF6600BBEB5DF97D6E1DA3FDFAA76E2FBCF69350E257F106F`, 2.647.318 bytes, Authenticode `NotSigned`) is op beide Windows-pc's geïnstalleerd. Beide GUI-processen draaien, beide sensorservices en beide PawnIO-services staan op `Running`, en dezelfde oude HA-entities tonen na installatie actuele CPU-temperaturen en netwerkmetingen. De 99 Desktop App-entity-ID's in de huidige registry hebben vingerafdruk `c36d730fda4adf609428ed19288675bade20c3b96721362fc7de13bbfe8704a6`; een directe voor/na-vergelijking van deze proef is niet vastgelegd. De macOS-volumeroute gebruikt [Apple's persistente volume-UUID](https://developer.apple.com/documentation/corefoundation/kcfurlvolumeuuidstringkey); op macOS en Linux ontbreekt nog een compileer-/runtimeproef. Bij onbekende of dubbele identiteit mag geen oude entity een andere fysieke meting krijgen.

HA op `render-unit` is handmatig op 1.0.11 gezet. De geauthenticeerde registratie-GET meldt die versie en een vergelijking van alle 129 Desktop App-entity-ID's vóór en na de HA-herstart leverde dezelfde vingerafdruk op. De Settings-footer toont de appversie en vraagt de integratieversie via de geauthenticeerde registratie-GET op. Daarna ontdekte de interactieve `beast-unit`-proef twee bestaande registraties voor dezelfde computer. Met back-ups en geverifieerde hardware-identiteit is de app teruggekoppeld aan de oorspronkelijke HA-entry; de oude CPU-, GPU- en uptime-IDs ontvangen nu live waarden. De tweede entry is via HA verwijderd en de entity registry bevat nog één `Beast-Unit` met 48 entities. De 1.0.8-app draait op beide Windows-hosts; CPU-temperatuur wordt zonder appschakelaar op de oude IDs doorgestuurd.

De exacte 1.0.5-installer is daarna op beide Windows-hosts uitgevoerd. Lokale installatiecontroles en live CPU-, GPU- en netwerkentities zijn bevestigd; de voor/na-fingerprint van de entityregistry is bij deze installatie niet vastgelegd. De hieronder genoemde 1.0.8-proeven zijn historische lokale proefbuilds en gelden niet als bewijs voor een publieke release.

Na de 1.0.5-installatie is de HA-integratie verder gecorrigeerd zonder nieuwe versiebump: ongewijzigde sensorregistratie werkt de actuele waarde nu bij zonder metadata opnieuw te schrijven; gewijzigde metadata bereikt bestaande entities; een wissel van sensor- naar binary-sensordomein op dezelfde ID wordt geweigerd; gedeeltelijke statusupdates wissen iconen en attributen niet. Mislukte platformsetup ruimt de eigen webhook, pending updates en zo nodig de timer op, terwijl een ander geladen apparaat blijft draaien. Een ontbrekende binaire meting wordt `unknown` in plaats van `off`. De lokale integratietests slagen **39/39**. Voor installatie op `render-unit` is de component met de drie relevante HA-opslagbestanden geback-upt (`/home/phill/ha-companion-backups/desktop-app-before-7788196-20260924.tar.gz`, SHA-256 `8ed9d143120bad2bba54d576d3fcb025af7faebdcf933de7aab9a366e54d6dd8`). Vier gewijzigde codebestanden zijn met identieke hashes geïnstalleerd; na herstart antwoordt de integratie-ping met HTTP 200, de gesorteerde HA-entity-ID-lijst heeft vóór en na exact dezelfde SHA-256 `99e229d2260f4c44efd4543c67fe604778d1bb46439542e44923ed9397583290`, en de containerlog toont geen fout/traceback in de eerste zeven minuten. Een echte metadatawijziging, uitval-/herstelproef en oudere clientpayload blijven open. Desktop-setup en Settings tonen bij veelvoorkomende fouten nu gelokaliseerde tekst; vier frontendtests en syntaxiscontroles slagen. De desktopbron blijft **1.0.5**, de integratiebron **1.0.11**.

Lokale kandidaatcontroles voor 1.0.8: HA-contracttests **32 geslaagd**, Rust **27 geslaagd en 1 optioneel overgeslagen**, frontend **4 geslaagd**, `cargo fmt` en Clippy geslaagd. De 1.0.8-upgrade en live app-naar-HA-CPU-temperatuurroute zijn op beide Windows-hosts geverifieerd. Deze controles bewijzen geen schone Windows-installatie zonder vooraf aanwezige driver, langdurige herstel-/uninstallmatrix, code signing, HACS-upgradepad of macOS/Linux-runtimegedrag. Geen van de oorspronkelijke 54 auditpunten is daarom automatisch als volledig gesloten gemarkeerd.

Historische regressiecontrole van vóór de eigen Rust-service (niet de huidige build): `cargo fmt --all -- --check` en `cargo clippy --locked --all-targets -- -D warnings` slaagden; `cargo test --locked --lib` **29 geslaagd, 1 optionele test overgeslagen** op zowel `phill-pc` als `beast-unit`; de optionele OS-kluis-roundtrip slaagde lokaal op `phill-pc` maar faalde via SSH op `beast-unit` (Windows-fout 1312); integratie-`pytest` **17/17**; frontend-`node --test tests/*.cjs` **4/4**. Een echte legacy-instelling op `phill-pc` migreerde zijn token uit `settings.json` naar de Windows-kluis terwijl de app en het HA-dashboard bleven werken. Een debugstart zonder frontendserver gaf terecht `localhost refused`; met de frontendserver op poort 1420 laadde de app Home Assistant normaal. De toenmalige CPU-temperatuurstate was `unknown`. De voormalige .NET-helper werd destijds gebouwd en getest, maar maakt geen deel meer uit van de huidige architectuur. Deze historische resultaten mogen niet als actuele releaseproef worden gebruikt.

Na een extra opstartproef op `phill-pc` met de actuele debugbinary bleven de app en de dashboardwebview actief, antwoordde de integratie-ping met HTTP 200, trad de overbodige autostart-waarschuwing niet meer op en werd de volledige dashboard-URL niet meer gelogd. Dit is een lokale rookproef met ontwikkelserver, geen geïnstalleerde releasebuild.

Na invoering van de ID-koppeling schreef een live start op `phill-pc` twee fysieke hardwarekoppelingen met hun bestaande lege suffix weg; het token bleef buiten `settings.json`, de HA-ping en dashboardwebview werkten, en de nieuwe log bevatte nul waarschuwingen/fouten. De 29 Rust-regressietests slaagden daarna ook op `beast-unit` via SSH. De herwerkte initiële registratie is wel gecompileerd en unit-getest, maar nog niet met een nieuwe HA-registratie doorlopen.

Aanvullende UI-controle: als Settings zijn backendgegevens niet kan laden, blijft de dialoog zichtbaar met een gelokaliseerde melding en is Opslaan uitgeschakeld. Als de dashboardwebview na sluiten niet terugkomt, blijft de dialoog open met een herstelmelding. Een mislukte sensorlijst verschijnt als melding in de lijst. De zes lokale frontendtests slagen. Dit is nog niet in de geïnstalleerde WebView op beide Windows-hosts getest.

De actuele **1.0.5**-NSIS-installer is opnieuw uit deze bron gebouwd (2.648.799 bytes, SHA-256 `2AE75210EF2C9266971FCD6C02FD44897E77AD831164FAB528FBB78F2DD89842`, Authenticode `NotSigned`). Een stille, verhoogde upgrade op `phill-pc` eindigde met code 0. De geïnstalleerde executable meldt 1.0.5; de app is opnieuw gestart uit `C:\Program Files\Home Assistant Companion\ha-companion.exe`, de twee sensordiensten draaien, de HA-integratie-ping antwoordt 200 en de dashboardwebview wordt volgens de applicatielog aangemaakt. Een enkele HTTP 502 tijdens de HA-herstart werd gelogd vóór de nieuwe appstart; dat is geen bewijs voor langdurig herstel. De volledige sensordata- en UI-proef van deze exacte build op beide hosts blijft open. De volledige Rust-libtest: **30 geslaagd, 3 opzettelijk overgeslagen**; `cargo fmt`, Clippy en zes frontendtests slagen.

Daarna is dezelfde installer (SHA-256 op `beast-unit` bevestigd) op `beast-unit` gestart vanuit een verhoogde SSH-sessie, na back-up van de instellingen. De remote `Start-Process`-aanroep gaf geen betrouwbare exitcode terug, maar de geïnstalleerde executable op **beide** Windows-hosts heeft dezelfde SHA-256 `7A385D9D902006BE233F72F64A84D6016D434BA3A3045825BC1A41D6DF8F5C44`, meldt 1.0.5 en draait in de interactieve sessie. Op beide hosts staan `HaCompanionSensors` en `PawnIO` op `Running`; de applicatielog meldt na start HA-ping 200 en een gemaakte dashboardwebview. De HA-recorder bevat na de installatie op de bestaande IDs actuele CPU-temperatuur, CPU-gebruik, geheugengebruik, GPU-temperatuur en uptime van beide hosts (bijvoorbeeld `sensor.phill_pc_cpu_temperature=50.0`, `sensor.beast_unit_cpu_temperature=57.0`). De gesorteerde HA-entity-ID-lijst heeft nog steeds SHA-256 `99e229d2260f4c44efd4543c67fe604778d1bb46439542e44923ed9397583290`. Hiermee is de app-naar-HA-route voor deze waarden op twee Windows-computers bevestigd; langdurige storing-, hotplug-, uninstall- en clean-installproeven blijven open.

Een gecontroleerde HA-containerherstart om 2026-09-24 15:57 lokale tijd (Unix-tijd `1790258247`) is daarna uitgevoerd **terwijl beide desktopapps bleven draaien**. De HA-integratie-ping keerde terug naar HTTP 200. In de recorder kregen de bestaande CPU-gebruik- en CPU-temperatuurentities van `beast-unit` om Unix-tijd `1790258266` weer numerieke waarden; `phill-pc` kreeg die om `1790258300`. Beide GUI-processen hielden hun oorspronkelijke starttijd en de entity-ID-fingerprint bleef `99e229d2260f4c44efd4543c67fe604778d1bb46439542e44923ed9397583290`. Dit bevestigt automatisch herstel na één HA-herstart op twee Windows-hosts, maar nog geen langdurige netwerkuitval of token-/registratieverlies.

Een extra HA-lifecyclecorrectie ruimt ook bij annulering van setup gedeeltelijk gestarte platforms en eigen runtimebronnen op. De integratietests slagen nu **40/40**. Alleen het gewijzigde `__init__.py` is na een aparte back-up (`/home/phill/ha-companion-backups/desktop-app-init-before-43332e3-20260924.py`) met SHA-256 `CE6EC1B639E23DD3C06B4E2A2F74ECB77B54D5D3829278A88D708C8F52BB423B` op `render-unit` geplaatst. Na herstart is de ping 200, de entity-ID-fingerprint onveranderd en hebben beide bestaande CPU-temperatuurentities weer een numerieke recorderwaarde. Er zijn geen `desktop_app`-fouten in de gecontroleerde opstartlog; fouten van een losse slaapkamerlamp-cloudautomatisering rond 16:00 zijn buiten deze integratie gevallen.

De actuele Rust-testbinary (SHA-256 `5C4991379EA9BEAE26BC4A289F3AFACF4B72D0E875EA86E7C907C773EBAA092F`) is ongewijzigd op `beast-unit` uitgevoerd. Op **beide** Windows-hosts slagen de 30 gewone tests en, afzonderlijk geactiveerd, de twee OS-afhankelijke proeven voor Windows-volume-GUID's en netwerkadapter-GUID's. Dit toont dat de identiteitsbron op beide hosts werkt, maar bewijst nog geen daadwerkelijke hotplug-/rebootstabiliteit.

De derde OS-afhankelijke Rust-proef (`os_credential_store_roundtrip`) slaagde lokaal op `phill-pc`. Op `beast-unit` is **dezelfde testbinary** via een tijdelijke interactieve Windows-taak voor de aangemelde gebruiker uitgevoerd; `LastTaskResult=0`. De taak is daarna verwijderd. Dit lost de eerdere fout 1312 van de niet-interactieve SSH-sessie als testbeperking op. De proef gebruikt en verwijdert een wegwerpcredential; langdurige opslag-/herstelproeven en Linux/macOS-kluizen blijven open.

De HA-integratiecorrectie `c029b61` rolt mislukte sensorregistratie-opslag terug, geeft HTTP 503 in plaats van een succesbevestiging en houdt gelijktijdige registraties en apparaatverwijdering via dezelfde lock gescheiden. De lokale integratietests slagen **43/43**, inclusief geforceerde opslagfout en overlappende registraties. Vóór installatie op `render-unit` is de component met config entries, entity registry en sensoropslag geback-upt naar `/home/phill/ha-companion-backups/desktop-app-before-c029b61-20260924.tar.gz` (SHA-256 `6aea93226337ad092a9f5ec41f013369da6ef776a1e00acbcfbeb375ced07391`). De drie gewijzigde Python-bestanden zijn met identieke SHA-256 op de server geplaatst en HA is herstart. De ping geeft HTTP 200, de vingerafdruk van de 99 gesorteerde Desktop App-entity-ID's bleef `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`, en de recorder bevat na de herstart verse numerieke CPU-temperatuur- en CPU-gebruikwaarden voor zowel `phill-pc` als `beast-unit`. Het hulpscript `scripts/verify_ha_recorder.py` leest hiervoor de SQLite-recorder alleen-lezen. De echte schijffout op de HA-server is niet opgewekt.

De desktopbron in commit `5483727` vergelijkt geregistreerde sensordescriptors en verstuurt bij een normale volledige cyclus alleen nieuwe of gewijzigde metadata; de volledige **waarden**update blijft iedere tiende cyclus lopen. Een HA-antwoord HTTP 409 wist de cache zodat een volgende volledige cyclus kan herstellen. `cargo test --locked --lib` meldt 33 geslaagd en 3 bewust overgeslagen; Clippy met `-D warnings` slaagt. Hieruit is opnieuw een NSIS-installer 1.0.5 gebouwd (2.661.292 bytes; SHA-256 `1BDC09C10801E965DE54AC810D5665AE67E9007051ECDEE9843F52FAA92BEA14`; Authenticode `NotSigned`). De instellingen zijn vóór upgrade op beide hosts apart geback-upt. Op `phill-pc` en `beast-unit` is de stille installatie geslaagd; beide geïnstalleerde executables hebben SHA-256 `E82A7AEBE9540BAE79E3AE75795CD24391C1F1B8C47D4EC448E75151FF305553` en bestandversie 1.0.5. De remote installer werd vóór uitvoering bytegelijk met SHA-256 gecontroleerd. Beide Windows-sensordiensten draaien en de GUI is op beide hosts opnieuw gestart, op `beast-unit` expliciet in interactieve sessie 1. De read-only HA-recorder gaf daarna verse numerieke CPU-temperatuur en CPU-gebruik voor beide bestaande apparaat-ID's; de 99 gesorteerde Desktop App-entity-ID's bleven op SHA-256 `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`. Dit toont één geslaagde upgrade-/herstelcyclus, maar nog geen langdurige storings-, hotplug- of prestatieproef. De installer is niet ondertekend en er is niets gepusht of gereleased. De macOS- en Linux-implementatie blijft onderdeel van de bron; hun runtimeproeven zijn op verzoek uitgesteld.

De read-only HA-configcontrole vond vóór de extra SEC-10-migratie een eigenaar op `beast-unit`, maar niet op de oudere `phill-pc`-entry. `scripts/verify_ha_credential_owner.ps1` las de reeds door de app gebruikte Windows-credential zonder die uit te schrijven en vroeg via Home Assistants `auth/current_user`-WebSocketopdracht uitsluitend de gebruiker en beheerderstatus op; de `phill-pc`-token bleek van een HA-beheerder. Na een registry-back-up (`/home/phill/ha-companion-backups/desktop-app-before-owner-migration-20260924.tar.gz`, SHA-256 `aadf3c0176303e74a34a1e25142d74758a12eccb7aaba8077c15b987340fc137`) heeft dezelfde bestaande token via de bestaande registratie-API de oude entry aan deze gebruiker gekoppeld. De API bevestigde **dezelfde** lokale webhook. Beide config entries hebben nu een eigenaar en de entity-ID-vingerafdruk bleef gelijk. Vervolgens is de integratiecode na componentback-up (`/home/phill/ha-companion-backups/desktop-app-before-a14dc19-20260924.tar.gz`, SHA-256 `efe73adc31e77286f2de32e2e5739fe20bae790aa00853b9c1c8052c72f87dfc`) met vergeleken bron-/staginghashes op `render-unit` geplaatst en HA herstart. Ping geeft HTTP 200; POST naar het verwijderde eventendpoint geeft HTTP 404. De beheerderregistratie levert ook met de nieuwe code nog dezelfde webhook op. De vier gecontroleerde CPU-recorderstates van beide hosts zijn daarna vers en numeriek; de 99 entity-ID's behouden SHA-256 `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`. De lokale integratietests slagen 44/44; een live negatieve test met een tweede niet-beherende HA-gebruiker ontbreekt nog.

De daaropvolgende definitieve lokale 1.0.5-build omvat ook de afsluitvlag, het gedeelde protocolfixture en de intervalvalidatie. De NSIS-installer is 2.653.156 bytes, SHA-256 `24BC482C77314519391FB5685F4F01EB478BA6FA9422D7C66B3D929C4C136076`, nog steeds `NotSigned`. Vóór de tweede upgrade zijn beide gebruikersinstellingen opnieuw geback-upt. De installatie op `phill-pc` en `beast-unit` is afgerond; beide geïnstalleerde executables hebben SHA-256 `A99C95EDA6EA6BD26E71B3F3E6ED5B74BB0E0DFF4C32E3929B8527E78AC107C6` en versie 1.0.5. Op beide hosts zijn `HaCompanionSensors` en `PawnIO` actief. De GUI is lokaal gestart en op `beast-unit` via een tijdelijke interactieve taak in sessie 1; die taak is verwijderd. Allebei de `settings.json`-bestanden hebben na upgrade dezelfde SHA-256 als hun vooraf gemaakte back-up. De HA-recorder toont daarna op beide bestaande apparaten verse numerieke CPU-temperatuur en CPU-belasting; de entity-ID-vingerafdruk blijft `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`. De desktopbron heeft 35 geslaagde Rust-tests en 3 bewust overgeslagen OS-/hardwaretests; Clippy slaagt. `sensor-core` heeft 2 en `sensor-service` 4 geslaagde tests, hun format/Clippy-controles slagen; `yarn audit` meldt nul advisories. Dit is nog geen bewijs voor clean install, uninstall, langdurige storingen of een getekende publieke release.

Op `beast-unit` is met de exacte laatste installer daarna ook een volledige stille uninstall en herinstall uitgevoerd. De uninstaller eindigde met code 0; de app-executable en `HaCompanionSensors` waren weg, de mogelijk gedeelde `PawnIO`-driver bleef bestaan en `settings.json` hield SHA-256 `BE5B35D43796247D8F587971938966323D4F0C28E55917214CB3BB6DACB4AA6B`. De herinstallatie eindigde met code 0, zette dezelfde executable-hash terug en startte beide sensordiensten. De GUI draaide daarna weer in interactieve sessie 1; de bestaande HA-CPU-entities kregen opnieuw verse numerieke metingen en de entity-ID-vingerafdruk bleef onveranderd. Dit bewijst de lifecycle met **bestaande gebruikersinstellingen** op één Windows-host, geen volledig schone gebruikersinstallatie of langdurige stabiliteit.

Daarna is op dezelfde host met `scripts/test_corrupt_settings_windows.ps1` een echte appstart met doelbewust ongeldige JSON in `settings.json` beproefd. Het script weigerde te starten als de vooraf geback-upte instellingen niet overeenkwamen, startte de app tijdelijk in sessie 1 en herstelde in `finally` de oorspronkelijke bytes en de normale GUI. De foutstart registreerde `Settings could not be loaded`; het ongeldige bestand bleef behouden in plaats van stilzwijgend te worden vervangen. Na herstel is `settings.json` opnieuw bytegelijk aan de back-up, de GUI draait en de bestaande `beast-unit`-CPU-entities ontvangen verse numerieke waarden. De entity-ID-vingerafdruk bleef ongewijzigd. Dit dekt een beschadigd bestand bij opstarten, nog niet een echte schrijffout midden in een instellingenwijziging.

Een daaropvolgende tijdelijke netwerkuitvalproef blokkeerde op `beast-unit` uitsluitend uitgaand verkeer van `ha-companion.exe` met een Windows-firewallregel. De GUI werd onder de blokkade opnieuw gestart; de app logde `Webhook connection failed` en behield de opgeslagen instellingen bytegelijk. `scripts/test_ha_outage_windows.ps1` verwijderde de regel in `finally` en bevestigde daarna dat de app nog draaide, de regel weg was en de instellingen ongewijzigd waren. Zonder nieuwe device-ID of handmatige Reconnect ontvingen de bestaande `beast-unit`-CPU-entities weer verse numerieke waarden; de entity-ID-vingerafdruk bleef `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`. Deze proef dekt een tijdelijke app-specifieke uitval en herstel, nog niet een HA-tokenintrekking, echte OS-shutdown of langdurige beschikbaarheidsmeting.

De read-only recordercontrole na herstel vond op `beast-unit` ook verse GPU-waarden: de modelentity toont `NVIDIA GeForce GTX 1060 3GB`, GPU-belasting `2.0` en temperatuur `48.0` op zowel de huidige als de bewaarde oude alias-entities. Die alias-entities zijn bewust behouden wegens de eis om bestaande HA-IDs en automatiseringen niet te verbreken; hun dubbele zichtbaarheid is een aparte UX-afweging, geen bewijs van twee fysieke NVIDIA-kaarten. De `Microsoft Remote Display Adapter` verschijnt afzonderlijk als displayadapter en krijgt geen verzonnen thermische meting.

Aanvullende broncontrole: een frontendcontracttest toetst nu alle 23 aangeboden sensoropties aan zowel Engelse als Nederlandse labels; de volledige frontendset slaagt 11/11. Schijfcapaciteitsattributen `total_gb` en `used_gb` worden als afgeronde decimale JSON-getallen opgebouwd in plaats van strings, met een eenhedenproef op 1 GB en 0,5 GB. De opstartcontrole toetst ook het opgeslagen toegangstoken met de geauthenticeerde Home Assistant-core-GET `/api/`: alleen HTTP 401 wordt als ongeldig token gemeld, terwijl tijdelijke netwerk-/serverfouten de registratie intact laten. Een doelbewust ongeldige token leverde op de live core-API HTTP 401. Bij een mislukte persistente webhook-wissing houdt de app voortaan zijn huidige in-memory registratie vast zodat RAM en schijf niet uiteenlopen. De Rust-set slaagt 37/37 met 3 overgeslagen hardware-/OS-proeven; formattering en Clippy slagen. De Windows-installer van deze bron is 2.661.129 bytes, SHA-256 `84D0F8A8C75E9255C19C05A403CFD6EA0BA69010D82EF8B8BB5CC6E27A668129` en Authenticode `NotSigned`. De verhoogde stille installatie eindigde op beide Windows-hosts met code 0; de geïnstalleerde executable heeft op beide SHA-256 `364951CE24E006010F504DA12D8F3CF9CFBC4EAD50E6A0985015B884ED7139A0` en versie 1.0.5. Vooraf gemaakte instellingenback-ups bleven bytegelijk aan de live bestanden; beide eigen sensordiensten en PawnIO draaien. De GUI draait op beide in interactieve sessie 1. De HA-recorder ontving vervolgens voor de bestaande CPU-gebruik- en CPU-temperatuurentities op beide hosts verse numerieke waarden. De 99 gesorteerde Desktop App-entity-ID's behouden SHA-256 `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`.

De daaropvolgende compatibiliteitscontrole heeft de tokenproef van de integratie-eigen registratieroute naar Home Assistants stabiele core-route `/api/` verplaatst. Zo blijft de opstartcontrole bruikbaar wanneer de desktopclient vóór de integratie wordt bijgewerkt. Een lokale HTTP-proef toetst zowel een geldige als een geweigerde token en verifieert het verzoekpad; de live core-route gaf met een ongeldige testtoken HTTP 401. De Rust-set slaagt nu 38/38 met 3 bewust overgeslagen OS-/hardwareproeven. Deze laatste bronwijziging is daarna opnieuw gebouwd en op beide Windows-hosts geïnstalleerd, zoals hieronder vastgelegd.

De laatste lokale 1.0.5-installer is 2.653.915 bytes, SHA-256 `5269AB6A29C836AF9B6D6DB8030F9AE1D51D2B02B960F1BEF4B48F1B798BDB25`, Authenticode `NotSigned`. De installatie eindigde op `phill-pc` en `beast-unit` met code 0; beide geïnstalleerde executables hebben SHA-256 `F334594888B4E6726584E8EEE08D31FCEAD7B8C0185E96D834B202F1D015AC2C` en bestandsversie 1.0.5. Op beide hosts bleven de vóór de upgrade geback-upte instellingen bytegelijk; de GUI draait in interactieve sessie 1, de eigen sensorservice en PawnIO draaien. De read-only HA-recorder toont verse numerieke CPU-belasting en CPU-temperatuur voor beide bestaande apparaten. Het gesorteerde register van 99 Desktop App-entity-ID's behield SHA-256 `a88575f2bf71631fa0963518bcf13aeefb65c4ab155998b30aeceb521d1a190e`. De schijfsensoren `sensor.phill_pc_disk_usage_c` en `sensor.beast_unit_disk_usage_c` leverden elk numerieke `total_gb`- en `used_gb`-attributen. Een 70 seconden durende WMI-procesmeting gaf voor de GUI 0,07% van één CPU-kern en 56,5 MiB werkset op `phill-pc`, respectievelijk 0,11% en 51 MiB op `beast-unit`; de eigen service gebruikte in beide vensters minder dan 0,005% van één kern bij afronding op twee decimalen en 5,5 MiB werkset. Dit is een kort momentopnamevenster, geen duurtest of meting op macOS/Linux. Er is niets gepusht of gereleased.

Voor ARCH-01 is na de bovenstaande Windows-installatie de sensorselectie samengebracht in `sensors/catalog.rs`. De 23 IDs, Engelse en Nederlandse keuzelabels en intervalvlaggen komen nu uit één backendlijst; de frontend heeft geen eigen vertaalduplicaat voor die keuzes meer. Een contracttest vergelijkt elke in de collector gebruikte `is_enabled`-sleutel met de catalogus, en een frontendproef toetst de directe taalwissel. De Rust-tests slagen 39/39 met 3 overgeslagen hardware-/OS-proeven; de frontendtests slagen 12/12. Deze wijziging is nog niet opnieuw in een installer gebouwd of op de twee Windows-hosts geïnstalleerd. De sensormetadata van de feitelijke HA-entiteiten en expliciete verbindingstoestanden zijn hiermee nog niet gecentraliseerd.

De cataloguswijziging is daarna als 1.0.5-installer gebouwd: 2.661.262 bytes, SHA-256 `0EE9C59FBFE54ECD353CDC0729BCDB95C43AE7B82ED62781CA5F54CDAAD8E1F3`, Authenticode `NotSigned`. De installatie eindigde op beide Windows-hosts met code 0. De geïnstalleerde executables hebben identieke SHA-256 `FCE73349040447034CE3D62824B33D1F5E3AA3917FBAC0F02B323A986D242841`, bestandsversie 1.0.5, draaiende eigen sensorservice en PawnIO, en een GUI in interactieve sessie 1. `phill-pc` behield exact zijn instellingenhash. Op `beast-unit` groeide uitsluitend `sensor_identity_map` met één fysieke koppeling; geen van de tien bestaande koppelingen of andere instellingen veranderde. De HA-recorder leverde daarna op beide bestaande apparaten verse numerieke CPU-belasting en CPU-temperatuur. Het HA-register bevat nu 101 Desktop App-entity-ID's: met `comm` vergeleken tegen de geback-upte lijst zijn alle 99 oude IDs aanwezig; alleen `sensor.beast_unit_beast_unit_network_rx_ethernet` en `sensor.beast_unit_beast_unit_network_tx_ethernet` kwamen erbij. De actuele gesorteerde-ID-hash is `c1f344c4c797acc7ffbb46a895120e3803ff36d53e4e96e92fe3283f073957f5`. Dit is een gecontroleerde uitbreiding, geen bewijs voor alle hotplug-/reboot- of platformscenario's.

Een echte Windows-autostartproef op `beast-unit` zette de instelling tijdelijk op `false`, startte de app in interactieve sessie 1 en zag de eigen `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`-vermelding verdwijnen. Het script herstelde de vooraf geback-upte instellingen bytegelijk in `finally`, startte de app opnieuw en zag dezelfde Run-vermelding terugkeren; de GUI draaide daarna en de bestaande CPU-entities ontvingen weer verse waarden. De eerste poging faalde uitsluitend in het testscript bij het uitlezen van een bewust afwezige registrywaarde; `finally` herstelde de oorspronkelijke instellingen en app ook toen. Het script is daarop aangepast en de volledige proef slaagde. Dit toetst de OS-configuratie, maar nog niet een echte gebruikersaanmelding na reboot.

De registratiecoördinatie is daarna vereenvoudigd: `AppState.is_registered` was een tweede veranderlijke kopie van de bewaarde webhook-ID. Dashboardinstellingen, directe updates, periodieke updates en herstelregistratie lezen nu dezelfde persistente webhookstatus. Daardoor kan een mislukte opslagactie geen `true`/`false` in RAM achterlaten die afwijkt van de bewaarde registratie. De Rust-tests slagen 39/39 en Clippy slaagt; deze laatste bronwijziging moet nog in een installer gebouwd en op beide Windows-hosts getest worden. Een afzonderlijke toestand voor verbinding, aflevering en providerstatus is hiermee nog niet volledig ontworpen.

Een langere storingstest op `beast-unit` blokkeerde de app 195 seconden met een tijdelijke, app-specifieke uitgaande firewallregel. Tijdens de blokkade registreerde HA `binary_sensor.beast_unit_online=off` en `sensor.beast_unit_cpu_usage=unavailable`; `phill-pc` bleef `on`. Het script zag transportfouten en behield de instellingenhash. De regel werd in `finally` verwijderd. Daarna werd dezelfde bestaande online-entity weer `on`, en CPU-belasting/temperatuur kregen nieuwe numerieke recorderwaarden; het script bevestigde dat de GUI bleef draaien en de firewallregel weg was. De oorspronkelijke 99 entity-ID's bleven aanwezig; de enige twee extra ID's zijn de eerder ontdekte Ethernet-sensoren. Dit dekt één echte timeout- en herstelcyclus, maar niet alle timer-/unload-/herstartscenario's.

De proef bevestigde ook een aparte frontendfout: na een *startup* tijdens een tijdelijke storing bleef het setupscherm anders staan nadat de achtergrondlevering was hersteld. De frontend hercontroleert die toestand nu iedere 30 seconden zolang het setupscherm vanwege onbereikbaarheid zichtbaar is. Bij een geldige verbinding opent hij het dashboard automatisch; bij een ingetrokken token of verdwenen webhook stopt de hercontrole en toont hij de passende melding. Niet-opgeslagen URL- of tokenwijzigingen worden niet overschreven. De nieuwe frontendtests slagen 14/14; de Rust-tests slagen 39/39 met 3 overgeslagen OS-/hardwareproeven. Deze frontend- en registratiebronwijzigingen zijn nog niet opnieuw gebouwd of op beide Windows-hosts geïnstalleerd.

De voorlopig laatste bron is opnieuw als 1.0.5-NSIS-installer gebouwd: 2.654.859 bytes, SHA-256 `206EA80442A23EED570B6030BC53F5374A55EC7DE85128BE0A467B5B5658467A`, Authenticode `NotSigned`. Op `beast-unit` eindigde de installatie met code 0; de geïnstalleerde executable heeft SHA-256 `613772D5EF385182E358E32E18F0C39670CD847094C357F3686704D23759374E`, versie 1.0.5, en draait in interactieve sessie 1 met beide sensordiensten. Het instellingenbestand behield zijn vooraf vastgelegde hash; HA ontving nieuwe CPU-belasting/temperatuur op de bestaande entities en de 101-ID-vingerafdruk bleef gelijk. De lokale UAC-installatie op `phill-pc` eindigde eveneens met code 0; de geïnstalleerde executable heeft dezelfde SHA-256 en versie, de instellingenhash bleef gelijk, de GUI draait in interactieve sessie 1 en beide sensordiensten zijn actief. De HA-recorder ontving daarna op beide bestaande apparaten verse numerieke CPU-belasting en CPU-temperatuur. De vergelijking met de vóór deze werkzaamheden geback-upte HA-entitylijst toont dat alle 99 oude IDs behouden zijn; alleen de twee al eerder vastgelegde Ethernet-entities op `beast-unit` zijn toegevoegd.

Een lokale SEC-06-logcontrole op `phill-pc` las de bestaande Windows Credential Manager-waarde uitsluitend in geheugen en vergeleek die en de bewaarde webhook-ID met zeven actieve en historische app-logbestanden, zonder de geheime waarden af te drukken. Het toegangstoken stond in geen ervan; de webhook-ID stond wel in `app.log.1` en een oud `.bak`-log. Die exacte waarde is in deze twee tekstlogs vervangen door `[redacted]`. Een tweede controle rapporteerde zeven logbestanden zonder token of webhook-ID. Het huidige `app.log` was al schoon. De tijdelijke gesaneerde werkbestanden van de herwerking zijn na hashvergelijking opgenomen in het oorspronkelijke, reeds gesaneerde logpad, zodat geen extra exemplaren overblijven. Dezelfde credential- en webhookvergelijking draaide daarna in de interactieve gebruikerssessie op `beast-unit`: twee logbestanden, geen token of webhook-ID gevonden.

Een eerste-gebruikproef op `beast-unit` verplaatste de bestaande instellingen tijdelijk, startte de geïnstalleerde 1.0.5-app in de interactieve gebruikerssessie en controleerde het nieuw aangemaakte profiel. De app maakte een nieuwe apparaatidentiteit aan, zonder server, webhook of token in het JSON-bestand. Het testscript herstelde de oorspronkelijke instellingen bytegelijk en startte de app opnieuw; de oorspronkelijke instellingenhash is `889DB220BC339F47F006EB66FA1F6D0B1F7F4E6BEE3D974091679FC542237BFC`. Daarna ontvingen de bestaande Beast-Unit CPU-belasting- en CPU-temperatuurentities opnieuw verse waarden. De eerste poging liep vast door een niet-ondersteunde .NET-overload in het testscript; handmatig herstel slaagde, waarna het script is gecorrigeerd en de volledige proef met alle controles slaagde. Dit bewijst een eerste start met een lege gebruikersconfiguratie, niet een volledige Windows-clean-installatie of HA-koppeling van een nieuw apparaat.

Een gecontroleerde SEN-04-proef op `beast-unit` schakelde CPU-temperatuur tijdelijk uit via een afzonderlijke kopie van de appinstellingen. De bestaande `sensor.beast_unit_cpu_temperature` kreeg in de HA-recorder de status `unknown` om 17:40:28 UTC; er werd geen nieuwe entity-ID voor deze sensor gemaakt. Het script herstelde het oorspronkelijke instellingenbestand bytegelijk (dezelfde SHA-256 als hierboven) en startte de app opnieuw. Dezelfde entity leverde daarna om 17:42:27 UTC weer een verse numerieke meting van 51,0 °C. De GUI draaide opnieuw in sessie 1 en het tijdelijke instellingenpad was weg. Dit toetst uit-/inschakelen en herstel op één Windows-host, maar nog niet hardware-hotplug of een volledige OS-reboot.

Een aanvullende opstartproef met de laatste geïnstalleerde build blokkeerde op `beast-unit` gedurende 90 seconden uitsluitend de uitgaande verbinding van de app, terwijl de GUI opnieuw werd gestart. Het app-log liet een transportfout zien; de instellingenhash bleef gelijk. Het script verwijderde de firewallregel in `finally`. Na herstel bleef de app actief en ontving de HA-recorder nieuwe waarden op de bestaande CPU-belasting- en CPU-temperatuurentities (9,9% en 49,0 °C om respectievelijk 17:45:44 en 17:46:44 UTC). De recorder toonde tijdens deze korte proef geen aantoonbare `offline`-overgang; daarvoor is de eerder uitgevoerde 195-secondenproef het bewijs. Een visuele bevestiging dat de setupweergave automatisch naar het dashboard omschakelt, ontbreekt nog.

Een resterende fout in REL-08/UI-02 is in de bron gecorrigeerd: als sensorvoorkeuren al duurzaam zijn opgeslagen maar de onmiddellijke HA-synchronisatie faalt, retourneert `save_settings` nu een expliciete `sensor_sync_pending`-status. De instellingenweergave meldt dan in de gekozen taal dat de instellingen bewaard zijn en de sensorwijzigingen nog niet door Home Assistant zijn bevestigd; de gebruiker moet de verbinding en registratie controleren. Zo wordt een bewaarde voorkeur niet ten onrechte een opslagfout genoemd, en wordt bij een verdwenen webhook geen automatisch herstel beloofd. Rust 39/39 tests (3 overgeslagen), Clippy, format en frontend 15/15 tests slagen. De gewijzigde uitkomst is met een frontendtest afgedekt; een opzettelijke live synchronisatiefout vanuit de Settings-UI is nog niet beproefd.

Deze laatste bron is opnieuw als **1.0.5**-NSIS-installer gebouwd: 2.650.426 bytes, SHA-256 `4E250C51D120D5DD0284B6C2119165DCC0142532FF4EF3EC5F5552F8DE2B891E`, Authenticode `NotSigned`. Beide Windows-upgrades eindigden met code 0. De geïnstalleerde executable heeft op beide machines SHA-256 `D40983615E69A55008C32164789F84FCA15157C6B69210577C9A2CE55694BE45`, versie 1.0.5; de oorspronkelijke instellingenhash bleef op beide machines gelijk, de GUI draait in sessie 1 en de eigen sensorservice en PawnIO staan op `Running`. Het verschil met de losse release-executable is byte voor byte onderzocht: drie bytes in de Tauri-bundelmarkering (`UNK` naar `NSS`), verder identiek. De HA-recorder ontving daarna op dezelfde entity-ID's verse CPU-belasting en CPU-temperatuur van `phill-pc` en `beast-unit`. De vergelijking van de HA-entitylijst met de voor deze werkzaamheden gemaakte back-up toont alle 99 oorspronkelijke ID's; alleen de twee eerder vastgelegde Ethernet-entities zijn toegevoegd. Dit bewijst de installatie en end-to-end basisroute van deze build, niet de nog openstaande signing-, HA-compatibiliteits-, volledige clean-install- en visuele toegankelijkheidsproeven.

Na precisering van de `sensor_sync_pending`-melding is opnieuw een 1.0.5-NSIS-installer gebouwd: 2.656.557 bytes, SHA-256 `63DABBBC4FDB37EC1B13DAA1CD500BD5A1ADD808CB93632BD8B5B4C7B343EBB7`, nog niet ondertekend. Op `phill-pc` eindigde de upgrade met code 0; de geïnstalleerde executable heeft SHA-256 `E1141B5672AA0CEE67A6D3F491C29A02D5A61F48A0862ED33E278FDFA74719FF`, versie 1.0.5. De instellingenhash bleef gelijk, beide sensordiensten draaien, de GUI draait in sessie 1 en de bestaande HA-CPU-entities ontvingen weer verse waarden. `beast-unit` wacht voor deze laatste tekstwijziging op zijn door de gebruiker aangekondigde herstart en daaropvolgende upgradecontrole; de eerdere build draaide daar al correct.
Na precisering van de `sensor_sync_pending`-melding is opnieuw een 1.0.5-NSIS-installer gebouwd: 2.656.557 bytes, SHA-256 `63DABBBC4FDB37EC1B13DAA1CD500BD5A1ADD808CB93632BD8B5B4C7B343EBB7`, nog niet ondertekend. Op `phill-pc` eindigde de upgrade met code 0; de geïnstalleerde executable heeft SHA-256 `E1141B5672AA0CEE67A6D3F491C29A02D5A61F48A0862ED33E278FDFA74719FF`, versie 1.0.5. De instellingenhash bleef gelijk, beide sensordiensten draaien, de GUI draait in sessie 1 en de bestaande HA-CPU-entities ontvingen weer verse waarden. Na de door de gebruiker uitgevoerde herstart is exact deze installer ook op `beast-unit` geïnstalleerd: exitcode 0, dezelfde executablehash en versie, behouden post-reboot instellingenhash `63DD92A1C75132E9361425AC1A79BC489A814EC8751CB41C3DBBD27A93E980CE`, beide sensordiensten actief en GUI in sessie 2. De HA-recorder ontving daarna op de bestaande CPU-entities van beide hosts verse numerieke waarden; `binary_sensor.beast_unit_online` kwam terug op `on`.

Een 70-secondenmeting van deze nieuwste build op `phill-pc` gaf voor de GUI 0,13% van één CPU-kern en 57,2 MiB working set; de eigen sensorservice mat afgerond 0,00% van één kern en 5,5 MiB. Proces-I/O bleef op de afgeronde 0,0 MiB. Dit is een kort idle-/dashboardprofiel op één host, geen volledige belasting- of batterijduurproef. Tijdens de herstart van `beast-unit` meldde de HA-recorder `binary_sensor.beast_unit_online=off` en de bestaande CPU-temperatuurentity `unavailable` om 18:05:57 UTC. Na de nieuwe boot om 18:11:04 UTC startte de app weer in de interactieve sessie en werd dezelfde online-entity om 18:12:35 UTC `on`; na installatie van de laatste kandidaat kwamen verse CPU-waarden binnen. Dit levert een echte reboot-/autostart-/HA-herstelproef op, zonder alle OS-afmeld- en geannuleerde-shutdownpaden af te dekken.

De reboot liet ook een nog niet afgeronde SEN-03-afweging zien. RDP-sessies `SESSIONID_0001` en `SESSIONID_0002` hebben verschillende Windows-PNP-identiteiten voor tijdelijke indirecte schermen en videoadapters. Daardoor kwamen twee nieuwe HA-entities (`display_resolution_1_2` en `gpu_model_1_2`) naast de bestaande RDP-entities; de fysieke GPU-identiteit en alle 99 oorspronkelijke entity-ID's bleven behouden. Extra Hyper-V-/WSL-adapters kregen eveneens eigen GUID-gebonden netwerkentities; één opnieuw aangemaakte `rnr-internal`-adapter kreeg terecht een nieuwe ID in plaats van de historie van een andere GUID over te nemen. Of tijdelijke RDP-apparaten voortaan gefilterd worden, is aan de gebruiker voorgelegd. Bestaande RDP-entities mogen bij afwezigheid niet aan een ander apparaat worden toegewezen.

Omdat de gebruiker nog geen afwijkende voorkeur voor sessiegebonden RDP-adapters heeft gegeven, filtert de volgende bronkandidaat Windows-`REMOTEDISPLAYENUM`-videoadapters en -schermen uit de WMI-collectie. Deze representeren de tijdelijke remote sessie, niet de fysieke GPU; de originele fysieke PNP-/NVML-identiteiten blijven zichtbaar. Een gedeelde helper toetst zowel de PNP-prefix als de bekende adapternaam wanneer de PNP-ID ontbreekt. Oude RDP-entity-ID's worden niet verwijderd of hergebruikt en horen na een volledig snapshot `unknown` te worden. Rust 40/40 tests (3 hardware-/OS-proeven overgeslagen), Clippy en format slagen. De gebruiker kan deze filterkeuze nog bijsturen; de nieuwe bron is nog niet geïnstalleerd of live tegen HA gecontroleerd.
Omdat de gebruiker nog geen afwijkende voorkeur voor sessiegebonden RDP-adapters heeft gegeven, filtert de nieuwe bron Windows-`REMOTEDISPLAYENUM`-videoadapters en -schermen uit de WMI-collectie. Deze representeren de tijdelijke remote sessie, niet de fysieke GPU; de originele fysieke PNP-/NVML-identiteiten blijven zichtbaar. Een gedeelde helper toetst zowel de PNP-prefix als de bekende adapternaam wanneer de PNP-ID ontbreekt. Oude RDP-entity-ID's worden niet verwijderd of hergebruikt. Rust 40/40 tests (3 hardware-/OS-proeven overgeslagen), Clippy en format slagen. De gebruiker kan deze filterkeuze nog bijsturen.

De RDP-filterkandidaat is als 1.0.5-NSIS-installer gebouwd: 2.660.691 bytes, SHA-256 `D2D5838C993B0293E4C96B4ECA716BB46508A0E56BFE062894CDE4F52F7560DA`, Authenticode `NotSigned`. Upgrades op beide Windows-hosts eindigden met code 0. De geïnstalleerde executable heeft op beide hosts SHA-256 `BF3D7FCC9D3E703D07B3FB03C701E5D0DD223FABDA621CF79C9FC49981ED5B1A`, versie 1.0.5; instellingenhashes bleven behouden en GUI, sensorservice en PawnIO draaien. Op `beast-unit` werden de twee eerdere, tijdelijke sessie-2-entities `sensor.beast_unit_beast_unit_display_resolution_1_2` en `sensor.beast_unit_beast_unit_gpu_model_1_2` na het volledige snapshot `unknown` op hun bestaande ID's. De fysieke scherm- en GPU-entities kregen tegelijkertijd verse waarden (1920×1080 en NVIDIA GeForce GTX 1060 3GB). Op `phill-pc` kregen de bestaande fysieke GPU- en schermentities eveneens verse waarden (NVIDIA GeForce RTX 3070 en 1920×1080); beide hosts stuurden nieuwe CPU-belasting en CPU-temperatuur. Een vergelijking met de oorspronkelijke HA-registry-back-up laat nog steeds alle 99 oude ID's zien; er kwamen bij deze filterinstallatie geen extra ID's bij. De eerder aangemaakte RDP-entities blijven als `unknown` in HA bestaan zodat hun historie en ID niet worden hergebruikt.

Na de reboot en de laatste installerupgrade is de logcontrole opnieuw in de interactieve Windows-gebruikerssessie van `beast-unit` uitgevoerd. De geplande, tijdelijke controletaak eindigde met code 0 en controleerde twee logbestanden: het token en de huidige webhook-ID kwamen in geen van beide voor; er was geen redactie nodig. De taak is na afloop verwijderd.

Voor BUILD-02 is de releaseworkflowconfiguratie zonder geheime sleutels in geheugen doorlopen: de drie signingproperties (`certificateThumbprint`, `digestAlgorithm`, `timestampUrl`) worden op het ingelezen JSON-object aangemaakt. De enige lokaal zichtbare geldige code-signingidentiteit op `phill-pc` is een zelfondertekend WDK-testcertificaat; dat is geen vertrouwde identiteit voor een publieke appinstaller. Daarom blijft de publieke Authenticode-ondertekening open en wordt dat testcertificaat niet voor de distributie gebruikt.

## Dependencyverificatie

Voor SEC-09 is een nieuwe foutmodus gevonden en verholpen: `serve` schakelde bij een bezette poort 1420 ondanks de eigen `--no-port-switching`-optie over naar een willekeurige poort op `::` (alle interfaces). De ontwikkelserver gebruikt nu alleen Node-core, bindt expliciet op `127.0.0.1:1420`, stopt bij `EADDRINUSE` en geeft uitsluitend de vooraf vastgelegde `dist`-assets door. Acht frontendtests slagen, waaronder weigering van bronbestanden en een bezette poort. In de lokale hostproef luisterde alleen `127.0.0.1:1420`, gaf `/` HTTP 200 en `/package.json` HTTP 404; een TCP-verbinding vanaf `beast-unit` naar de LAN-interface van `phill-pc` op poort 1420 mislukte. De ongebruikte `serve`-afhankelijkheid en zijn lockfileboom zijn verwijderd.

Na deze verwijdering slagen `yarn install --frozen-lockfile --offline` en `yarn audit --summary`: **0 advisories, 13 geaudite pakketten**. Dit zegt niets over de eerder vastgelegde Rust-waarschuwingen of de vendored frontendassets.

Na verversen van de bestaande, toegestane versieranges rapporteert `yarn audit` **0** advisories. `cargo audit 0.22.2`, opnieuw uitgevoerd op de huidige lockfile inclusief keyring, rapporteert nog **RUSTSEC-2021-0119** voor `nix 0.19.1` via `battery 0.7.8`. `cargo tree --target all -i nix@0.19.1` toont die route; `cargo tree --target x86_64-unknown-linux-gnu -i nix@0.19.1` en `--target aarch64-apple-darwin` tonen geen route. De battery-bron schakelt `nix` alleen in voor FreeBSD en DragonFlyBSD, buiten de geclaimde Windows/macOS/Linux-release. De CI-scan negeert uitsluitend dit ID met gedocumenteerde reden. Nieuwe kwetsbaarheidsmatches falen de releasebuild. De scan meldt daarnaast acht onderhouds-/unsoundnesswaarschuwingen in transitieve dependencies (`mach`, `proc-macro-error`, vijf `unic-*`-crates en `glib`); de CI-configuratie toont ze maar faalt daar nog niet op. Hun bereik moet per doelplatform en API-pad worden beoordeeld voordat DEP-01 kan sluiten.

## Forgejo-staging en kleine vensters

De private repository `FiLL/HA-Companion-App` is via de bestaande SSH-toegang aangemaakt en als `origin` ingesteld; `main` is zonder tag of release gepusht. Forgejo Actions staat aan. De voor het account beschikbare runners melden labels `windows-latest`, `ubuntu-latest` en `macos-latest`; een daadwerkelijke workflowrun is pas bewijs zodra de resultaten zijn gecontroleerd. De Forgejo-verificatieworkflow staat daarom apart onder `.forgejo/workflows/verify.yml`; de handmatige publieke releaseworkflow is niet gestart.

Bij een CSS-viewport van 320 × 240 (vergelijkbaar met 640 × 480 bij 200% schaal) past de Settings-dialoog nu binnen de viewport: via Edge DevTools is `innerWidth=320`, `document.scrollWidth=320`, en de sluitknop ligt volledig binnen de dialoog. Een screenshot bevestigt dat ook Annuleren en Opslaan zichtbaar zijn en de inhoud afzonderlijk scrolt. De setupkaart blijft horizontaal binnen de viewport en kan verticaal scrollen. Dit is een gecontroleerde browserweergave, nog geen native WebView2-/schermlezerproef.

De eerste Forgejo-runs brachten platformfouten aan het licht die lokaal op Windows verborgen bleven. De Windows-runner heeft geen `vswhere.exe`/Visual C++ Build Tools en kan daarom `link.exe` niet leveren voor de MSVC-build; de workflow probeert een aanwezige installatie via `vcvars64.bat` te laden en faalt nu met een gerichte melding als die ontbreekt. Op Linux en macOS maakte `-D warnings` een uitsluitend op Windows gebruikte temperatuurhulpfunctie zichtbaar; die functie is op Windows begrensd. De Linux-Rusttests kwamen vervolgens tot 37 geslaagde tests en één falende test die een Windows-padseparator verwachtte; deze test is platformneutraal gemaakt. Beide broncorrecties slagen lokaal voor format, Clippy en 40/40 gewone Rusttests. Een nieuwe volledige runneruitkomst en de Windows-buildtoolinstallatie blijven nodig voordat BUILD-04/06 als geslaagd mogen gelden.

Forgejo-run 13 op commit `40a2327` heeft op `ubuntu-latest` de frontend-, format-, Clippy- en Rustcontroles voltooid: 15 frontendtests en 38 Rusttests geslaagd, één OS-kluisproef bewust overgeslagen. De Windows-job van dezelfde run faalde vóór de Rustbuild met de expliciete melding dat de Visual C++ Build Tools-workload ontbreekt. De macOS-job was bij deze update nog actief; diens uitkomst is hier nog niet als bewijs opgenomen.

De Windows NSIS-installer met de kleine-vensterfix is opnieuw lokaal gebouwd als 1.0.5 (2.653.442 bytes, SHA-256 `7CBD90FDC1B743506C9A8D5B462184BDD91E005AEC58F8246A4074C261676EAF`, Authenticode `NotSigned`). Vóór de upgrade op `phill-pc` is `settings.json` bytegelijk veiliggesteld. De verhoogde stille installer eindigde met code 0; de geïnstalleerde executable meldde versie 1.0.5 en SHA-256 `7B435E45F5B2041D93ACB5DE3ED64AA8D1AE0B89197B99EE4D282F79B398B234`. De instellingenhash bleef `0898B5158AD0C23E46B0156DDF10BAF8A17437DB66597DEDB6A76D06F5FA2061`, beide sensordiensten draaiden en de opnieuw gestarte GUI liep in interactieve sessie 1. De alleen-lezen HA-recorder toonde daarna verse numerieke waarden op de bestaande `sensor.phill_pc_cpu_usage` en `sensor.phill_pc_cpu_temperature`. De CSS-fix is hiermee in de geïnstalleerde binary opgenomen; een native kleine-venster-/schermlezerproef blijft open.

Dezelfde installer is ook op `beast-unit` geïnstalleerd nadat zijn SHA-256 op die host was gecontroleerd. De SSH-sessie bleek een verhoogd Windows-token te hebben; de eerdere negatieve bevoegdheidscontrole gebruikte per ongeluk de Unix-`whoami` uit het SSH-pad. De stille installatie eindigde met code 0. De geïnstalleerde executable heeft dezelfde SHA-256 `7B435E45F5B2041D93ACB5DE3ED64AA8D1AE0B89197B99EE4D282F79B398B234` en versie 1.0.5. De instellingen en hun lokale back-up hebben beide onveranderd SHA-256 `63DD92A1C75132E9361425AC1A79BC489A814EC8751CB41C3DBBD27A93E980CE`. `HaCompanionSensors` en `PawnIO` draaien. De GUI was vóór de upgrade niet actief; via de bestaande interactieve starttaak draait hij nu in sessie 2. De HA-recorder meldde daarna `binary_sensor.beast_unit_online=on`, `sensor.beast_unit_cpu_usage=53.2` en `sensor.beast_unit_cpu_temperature=70.0` als verse waarden op de bestaande entity-ID's.

Bij hercontrole van Forgejo-run 13 op commit `40a2327` staan zowel `macos-latest` als `ubuntu-latest` op `success`; `windows-latest` is op de C++-toolchainvoorwaarde blijven steken. De Forgejo-workflow accepteert nu ook een reeds door de runner geconfigureerde `cl.exe`/`link.exe`-omgeving zonder `vswhere.exe` en zoekt anders een Visual Studio-installatie via `vswhere.exe`. Daarmee is de voorcontrole minder strikt, maar een Windows-runner zonder MSVC en Windows SDK kan nog steeds niet bouwen. De vereiste runnerinstallatie staat in de README; een geslaagde Windows-CI-run is nog open.

De direct na commit `149ab0f` gestarte Forgejo-run 805 bevestigde dat op de Windows-runner ook deze minder strikte controle faalt: er is noch een volledig geconfigureerde C++-omgeving, noch een bereikbare `vswhere.exe`. De job stopt daarom terecht vóór de Rustbuild. De runnerhost is niet `phill-pc` of `beast-unit` en is vanuit deze workspace niet bereikbaar om de ontbrekende workload daar te installeren. Windows-CI blijft open tot de runnerbeheerder die host inricht en de job opnieuw draait.

Een latere herkansing van die Windows-job bereikte de Rustbuild wel. De eerder ontbrekende C++-toolchain is dus inmiddels op de runner beschikbaar. De nieuwe fout was projectgebonden: Tauri las op Windows `tauri.windows.conf.json` en vereiste de gegenereerde map `resources/sensor-service` al tijdens `cargo clippy`, terwijl de workflow de sensorservice pas daarna bouwde. De Forgejo- en GitHub-verificatieworkflows bouwen de geïntegreerde service nu vóór de Tauri-Rustcontroles; de GitHub-releasecontrole doet hetzelfde. Op Windows slaagden de bestaande `build-sensors.js`, Tauri-Clippy en 15 frontendtests lokaal na deze volgordeaanpassing. Een nieuwe schone runnerproef moet nog aantonen dat de hele Windows-job nu slaagt.

Forgejo-run 806 passeerde vervolgens de servicebuild, Tauri-Clippy, Tauri-Rusttests en PawnIO-verificatie. De Windows-job faalde pas bij sensor-core-Clippy: de nieuwere runner-Rust noemt de `unsafe`-blokken om `__cpuid` overbodig, terwijl de lokaal geïnstalleerde toolchain die blokken nog verplicht stelt. De lint-uitzondering is nu beperkt tot de CPU-identificatiefunctie; beide sensorcrates slagen lokaal voor format, Clippy met `-D warnings` en hun tests, en de releasebuild van de service slaagt. De volgende schone Windows-run blijft het beslissende CI-bewijs.

Forgejo-run 807 kwam daarna door Tauri (40 geslaagde Rusttests, drie overgeslagen), de PawnIO-resourceverificatie en sensor-core (twee tests), maar faalde bij sensor-service-Clippy. De nieuwere Clippy prefereert voor een vaste bytepaarbreedte `as_chunks::<2>()` boven `chunks_exact(2)`; die omzetting is toegepast. Op de lokale Windows-toolchain slagen sensor-service-format, Clippy met `-D warnings` en vier tests. Een geslaagde volledige Windows-run is nog niet vastgesteld.

Forgejo-run 808 op commit `c524185` heeft **windows-latest: success**. De job bouwde de gebundelde sensorservice vóór Tauri-Clippy, controleerde de PawnIO-resources en voltooide 15 frontendtests, 40 Tauri-Rusttests (drie bewust overgeslagen), twee sensor-coretests en vier sensor-servicetests. Ook de format- en Clippy-stappen met `-D warnings` slaagden. Lokaal slaagden alle drie Windows-crates daarnaast met Rust 1.98.1-Clippy. Bij deze vastlegging waren `macos-latest` en `ubuntu-latest` van dezelfde run nog actief; de Linux-job wachtte op het ophalen van zijn runnercontainer. Eerdere macOS- en Linux-jobs op commit `40a2327` waren wel geslaagd, maar dat vervangt geen uitkomst voor commit `c524185`.

Eindcontrole van precies dezelfde Forgejo-run 808: `windows-latest`, `macos-latest` en `ubuntu-latest` staan alle drie op **success**; de run als geheel staat op **success**. Daarmee is de CI-verificatie van commit `c524185` op alle drie platformrunners geslaagd. Dit is een bouw-/testresultaat, geen bewijs voor interactieve installatie, sensormetingen of HA-koppeling op macOS en Linux.

## Releasekandidaat en vervolgstappen op 25 september

De actuele bron na de groene CI is lokaal opnieuw als Windows NSIS **1.0.5** gebouwd. De installer is 2.654.282 bytes, SHA-256 `5E5F57467CF6E29ABF3EBF02B3C150CFE02521A1C86EB382F67C5E0D694411F8` en Authenticode `NotSigned`. Exact deze installer is na een bytegelijke instellingenback-up op `phill-pc` en `beast-unit` geïnstalleerd; beide stille installaties eindigden met code 0. De geïnstalleerde executable heeft op beide hosts SHA-256 `61A83AA19D3BFA5A34110EB820D5C451A23006683DB5595C885343CB87F72D1B` en versie 1.0.5. De instellingenhashes bleven respectievelijk `0898B5158AD0C23E46B0156DDF10BAF8A17437DB66597DEDB6A76D06F5FA2061` en `63DD92A1C75132E9361425AC1A79BC489A814EC8751CB41C3DBBD27A93E980CE`; `HaCompanionSensors` en `PawnIO` staan op beide hosts op `Running`. De GUI draait opnieuw in interactieve sessie 1 op `phill-pc` en sessie 2 op `beast-unit`. De alleen-lezen HA-recorder liet na deze upgrade verse numerieke waarden op de bestaande `sensor.phill_pc_cpu_usage`, `sensor.phill_pc_cpu_temperature`, `sensor.beast_unit_cpu_usage` en `sensor.beast_unit_cpu_temperature` zien. De ongewijzigde online-state heeft geen nieuwe recorderregel nodig; deze waarde is daarom niet als verse heartbeat geteld. De live HA-manifestversie op `render-unit` is 1.0.11 en de lokale integratiesuite slaagt met **44/44** tests. De publieke HACS-integratierelease is bij controle nog 1.0.10 en de publieke desktoprelease nog 1.0.4.

De volgende releasepoort is niet een versiebump maar bewijs voor de resterende auditacceptatie: (1) fout- en herstelproeven op beide Windows-hosts, inclusief netwerkuitval, app-/HA-herstart, afmelding en geannuleerde shutdown; (2) een gecontroleerde schone installatie, uninstall/herinstall en controle van instellingen, driver/service-ACL en oude entity-ID's; (3) echte HA-integratieproeven voor lifecycle, metadata, oude payloads en HACS-upgrade met back-up en rollback; (4) native webview-/TLS-/toegankelijkheidsproeven en een laatste bron-/dependency-/logcontrole; (5) een distributiebesluit over vertrouwde installerondertekening, waarna pas de afzonderlijke HACS- en desktoprelease worden gepubliceerd. De macOS- en Linux-implementatie blijft in de bron en is in CI getest; de gebruiker heeft hun interactieve runtimeproeven voor deze ronde uitgesteld. Geen van deze open proeven krijgt door de groene CI automatisch status `Afgerond`.

### Vervolgproeven op 25 september

De exacte installer met SHA-256 `5E5F57467CF6E29ABF3EBF02B3C150CFE02521A1C86EB382F67C5E0D694411F8` is op beide Windows-hosts ook volledig verwijderd en opnieuw geïnstalleerd. Beide uninstall- en reinstallprocessen eindigden met code 0. Tijdens de uninstall verdween de applicatie-executable en de eigen sensorservice, bleven de gebruikersinstellingen bytegelijk en bleef de gedeelde PawnIO-driver aanwezig. Daarna kwamen de services terug, bleef de geïnstalleerde executable op beide hosts bytegelijk aan de gecontroleerde releasekandidaat en publiceerden de bestaande HA-entities weer verse numerieke CPU-gebruiks- en CPU-temperatuurwaarden. De sensorservicebinary is eigendom van Administrators; Users heeft uitsluitend lees-/uitvoerrechten, SYSTEM en Administrators hebben volledige rechten. Dit bewijst de upgrade-/uninstall-/reinstallroute op bestaande installaties. Een installatie vanuit een geheel lege Windows DriverStore met precies deze kandidaat is nog niet bewezen.

Een tijdelijke programmagebonden Windows-firewallregel blokkeerde de app op `beast-unit` aantoonbaar: het app-log registreerde tijdens de 75 seconden blokkade een verbindingsfout; na het verwijderen van de regel bleef de instelling bytegelijk, draaide de GUI in sessie 2 en leverden de bestaande HA-entities weer nieuwe metingen. Op `phill-pc` gaf dezelfde proef **geen** verbindingsfout en bleven tijdens de vermeende blokkade HA-updates binnenkomen. Die phill-proef is daarom **onbeslist**, geen geslaagde uitvaltest. Het script leest nu alleen logregels die tijdens zijn eigen run zijn geschreven en eindigt met een fout als geen nieuwe verbindingsfout is aangetoond. De oorzaken van de ineffectieve firewallregel en herstel na een bewezen netwerkuitval op phill-pc blijven open.

De aangescherpte netwerkuitvalproef is daarna opnieuw op `beast-unit` uitgevoerd met uitsluitend nieuwe logregels als bewijs. De run eindigde met code 0: `TransportErrorObserved=true`, `SettingsPreserved=true`, `AppRunning=true` en `FirewallRuleGone=true`; de HA-recorder toonde nadien verse CPU-gebruiks- en temperatuurwaarden. De autostart-uit/aan-proef slaagde vervolgens op **beide** Windows-hosts in de gewone gebruikerscontext: zonder autostartinstelling verdween de Run-vermelding, na herstel kwam die terug, en de oorspronkelijke instellingenhash bleef gelijk. De GUI draaide na afloop weer in sessie 1 op `phill-pc` en sessie 2 op `beast-unit`. Een eerste beast-run weigerde terecht een verouderde, afwijkende testback-up en wijzigde niets; met een nieuwe, afzonderlijke back-up slaagde de proef. Een voorafgaande verhoogde startpoging op phill bereikte de test niet; zij heeft de instellingen of Run-vermelding niet gewijzigd en telt niet als testresultaat.

Een tweede foutinjectie verving op beide hosts het instellingenbestand tijdelijk door ongeldig JSON. De app liet het corrupte bestand staan en logde op beide hosts de expliciete laadfout (`CorruptFilePreserved=true`, `LoadErrorLogged=true`), dus er ontstond geen stilzwijgende nieuwe registratie. Het testscript herstelde daarna in zijn `finally`-blok bytegelijk de oorspronkelijke instellingen en startte de GUI opnieuw in de juiste gebruikerssessie. De HA-recorder liet vervolgens op beide bestaande apparaten opnieuw verse CPU-gebruiks- en temperatuurwaarden zien. Deze proef toont herstel na een corrupte configuratie met herstel van de back-up, niet automatisch herstel van corrupte gebruikersdata zonder back-up.

Om de op `phill-pc` onbesliste programmagebonden firewallproef te vervangen is de HA-container daarna gecontroleerd 85 seconden stilgezet en via een shell-`trap` altijd opnieuw gestart. Beide actieve apps logden tijdens de echte serveruitval tweemaal een HTTP 502 van de reverse proxy. Zonder handmatige herregistratie kwamen op beide bestaande apparaten weer verse numerieke CPU-gebruiks- en temperatuurwaarden binnen. De instellingenhashes bleven respectievelijk `0898B5158AD0C23E46B0156DDF10BAF8A17437DB66597DEDB6A76D06F5FA2061` en `63DD92A1C75132E9361425AC1A79BC489A814EC8751CB41C3DBBD27A93E980CE`; de GUI's draaiden nog in sessie 1 en 2. Alle 109 vóór de uitval vastgelegde combinaties van entity-ID, unieke ID en config-entry-ID bleven aanwezig, zonder toevoegingen. Dit bewijst automatisch herstel na een echte HA-uitval voor beide Windows-clients; het maakt de ineffectieve lokale firewallregel niet achteraf tot een geslaagde proef.

Een 70 seconden lange normale-runtimeproef op beide pc's mat CPU-tijd van de desktopapp en de sensorservice via de Windows-procescounters vóór en na de interval; het gerapporteerde CPU-percentage is gedeeld door de logische processorcount van de host. Op `phill-pc` gebruikte de app 0,078 CPU-seconde (0,007% van de totale hostcapaciteit) en 47,9 MiB working set; op `beast-unit` 0,203 CPU-seconde (0,005%) en 50,2 MiB. De eigen sensorservice kwam in die korte periode onder de resolutie van de procescounter uit en gebruikte 5,5/5,6 MiB working set. Dit is één rustige updatecyclus per host, geen piek-, netwerk-, disk-I/O- of langdurige regressiemeting; PERF-01/02 blijven daarom gedeeltelijk open.

De CPU-temperatuursensor is vervolgens op **beide** Windows-hosts tijdelijk uitgeschakeld via een gecontroleerde kopie van hun settings. `phill-pc` had hiervoor een expliciete `true`-voorkeur, `beast-unit` gebruikte de impliciete standaard; beide situaties zijn getest. De bestaande `sensor.phill_pc_cpu_temperature` en `sensor.beast_unit_cpu_temperature` werden tijdens de proef `unknown` en niet opnieuw aangemaakt. Alle 109 vastgelegde entity-ID-/unique-ID-/config-entry-combinaties bleven exact gelijk. Na bytegelijk herstel van de originele settings en herstart van de GUI leverden dezelfde entities weer verse numerieke temperatuurwaarden. Dit sluit de concrete uitschakel-/herstelroute voor deze sensor op beide hosts af, maar is geen fysieke hotplugproef voor schijven, netwerken of identieke GPU's.

De huidige 1.0.11-integratie op `render-unit` is vergeleken met de kandidaat in de eigen integratierepository: alle 15 Python-/JSON-bestanden hebben dezelfde SHA-256. De vier JSON-bestanden zijn geldig, de elf Pythonbestanden zijn syntactisch geldig en de lokale integratiesuite slaagt met 44/44 tests. Vóór een gecontroleerde Home Assistant-herstart zijn 109 entities voor `phill-pc` en `beast-unit` met hun entity-ID, unieke ID en config-entry-ID vastgelegd. Na de herstart ontbreekt of verschijnt geen enkele tuple; beide clients sturen weer verse CPU-gebruiks- en temperatuurmetingen. `phill-pc` kreeg bij het opstarten van HA één ongeldige webhookbevestiging, maar herstelde op de volgende updatecyclus zonder handmatige registratie. De eerder gebruikte registry-back-up van 24 september bevat nog de inmiddels verwijderde dubbele `beast-unit`-entry en is daarom geen geldige nulmeting voor deze vergelijking. Een echte update van de publieke HACS-versie 1.0.10 naar 1.0.11 blijft open zolang 1.0.11 niet gepubliceerd is; de live handmatig geplaatste code en een HA-herstart vervangen die proef niet.

De dependencycontrole meldt nul kwetsbaarheden in de 13 JavaScript-pakketten. `cargo audit` scant 577 Rust-crates en meldt, afgezien van `RUSTSEC-2021-0119` in `nix 0.19.1` (alleen FreeBSD/DragonFly-route via `battery`, buiten de drie doelplatformen), acht waarschuwingen voor verouderde/ongehandhaafde dependencies. Daaronder valt `glib 0.18.5` met een unsoundness-advisory; de afhankelijkheidsboom toont die versie op Linux via de Tauri GTK/WebKit/tray-stack, niet in de Windows-targetboom. Dit is voor Linux nog een te beoordelen upstream-afhankelijkheid en geen reden om de Rust-scan als volledig schoon te beschrijven. De frontend heeft 15 tests, de Tauri-app 40 geslaagde Rusttests met drie bewust overgeslagen Windows-tests, de twee sensorcrates twee en vier tests, en de integratie 44 tests. De NSIS-installer is nog `NotSigned`; een publiek vertrouwde code-ondertekenidentiteit is niet aangetroffen. Native 200%-zoom/schermlezer, externe webview-IPC-afwijzing, negatieve TLS-certificaatproef, af-/aanmelding en echte OS-shutdown zijn nog geen bewezen acceptatieproeven.

De publieke GitHub-releaseworkflow controleert voortaan zowel de Windows-appbinary als de NSIS-installer op een geldige Authenticode-handtekening voordat hij artefacten uploadt. De controle is op de huidige ongetekende lokale kandidaat negatief beproefd en weigert die terecht. De gebruiker publiceert namens een geregistreerde onderneming buiten Azure; [Microsoft Artifact Signing Public Trust](https://learn.microsoft.com/en-us/azure/artifact-signing/quickstart) is voor EU-organisaties beschikbaar, maar vereist een [betaald Azure-abonnement](https://learn.microsoft.com/en-us/azure/artifact-signing/faq), organisatieverificatie, een certificaatprofiel en een geautoriseerde signer. Het getoonde Azure-account heeft nu expliciet **geen abonnement**; op deze pc is bovendien geen Azure CLI of bruikbare publieke code-ondertekenidentiteit aanwezig. Er is geen certificaat aangevraagd, geen betaalde Azure-resource gemaakt en geen publieke release gepubliceerd. De huidige workflow ondersteunt import van een PFX; voor een Artifact Signing-profiel moet de workflow na accountinrichting nog op de [cloudsigner](https://v2.tauri.app/distribute/sign/windows/) worden aangesloten en met een echte ondertekende build worden gevalideerd.

**Releasebeleidswijziging op 25 september:** na onderzoek van de SignPath-aanvraag en het ontbreken van een direct bruikbare gratis, publiek vertrouwde code-ondertekenroute heeft de gebruiker de Windows-waarschuwing bij ongetekende distributie geaccepteerd. De GitHub-releaseworkflow accepteert nu uitsluitend twee geldige handtekeningen of twee `NotSigned`-bestanden; gemengde en ongeldige uitkomsten falen. De status wordt als `windows_authenticode_status` in het release-manifest opgenomen en bij `NotSigned` in de releasenotities uitgelegd. De lokale 1.0.5-app en installer zijn beide daadwerkelijk `NotSigned`. Dit besluit neemt alleen de ondertekenblokkade weg; alle andere open acceptatieproeven, inclusief de HACS-upgradeproef, blijven staan. Er is nog geen publieke release gestart.

Na expliciete toestemming voor de nieuwe versie zijn desktopcommit `04ba853` en integratiecommit `62caf86` naar hun publieke GitHub-hoofdbranches gepusht. De integratie is als [1.0.11](https://github.com/Fill84/ha-integration/releases/tag/1.0.11) gepubliceerd. Voor de HACS-upgrade is een volledige HA-configuratieback-up op `render-unit` gemaakt (`backup/companion-2026-09-25-pre-hacs-1.0.11.tar.gz`, SHA-256 `2d29da850228b1cced99728f508d1e29a2e3c4f2a866c21bb342a88f1945c826`). HACS meldde vóór installatie `available=1.0.11`, `installed=1.0.10`; na de HACS-download en HA-herstart melden HACS en het geïnstalleerde manifest beide 1.0.11. De HA-configuratiecontrole eindigde met code 0 en de integratie-ping gaf HTTP 200. Alle 109 bestaande Desktop App-combinaties van entity-ID, unique-ID en config-entry-ID bleven exact gelijk (SHA-256 `e9c3d4ea9ae0d79754a62e84f4b1fdcbc5f2f4d604757fa06409516526d1cd65` vóór en na). Beide online-entities stonden daarna op `on`; de HA-recorder bevatte om 13:06 UTC verse numerieke CPU-gebruiks- en temperatuurwaarden voor `phill-pc` en `beast-unit`. Hiermee is het concrete HACS-upgradepad voor deze omgeving bewezen. De desktopbronverificatie op GitHub-run `36138045057` slaagde op Windows, macOS en Linux; dit zijn geen interactieve macOS-/Linux-runtimeproeven. De desktopreleaseworkflow maakt voortaan eerst een conceptrelease om het exacte Windows-artefact op beide pc's te testen vóór publicatie.

De eerste desktoprelease-run `36139141207` bouwde Windows succesvol maar faalde op macOS/Linux omdat de releaseworkflow de frontendtest vóór `build-web.js` uitvoerde. Na correctie slaagde [run `36139472592`](https://github.com/Fill84/HA-Companion-App/actions/runs/36139472592) volledig: Windows x64 NSIS, macOS aarch64 DMG, Linux x86_64 DEB/RPM/AppImage en de integratiezip werden gemaakt. De zeven aangeleverde bestanden zijn allemaal bytegelijk tegen `release-manifest.json` gecontroleerd. De Windows-installer heeft SHA-256 `06523e535ec6df419837f79f748f24f1343f7845cbdd51cf659e55f35ce0df50` en Authenticode-status `NotSigned`; dat staat ook in het manifest en de releasenotities.

Exact die GitHub-installer is op `beast-unit` en `phill-pc` uitgevoerd, beide met exitcode 0. De geïnstalleerde executable heeft op beide hosts SHA-256 `B65D82EBE9884E467D3D78B96F13AF12E542B8C0192AC1200DECC33CEBFFEBC0` en versie 1.0.5. De oorspronkelijke instellingen bleven op beide hosts bytegelijk (`0898B5158AD0C23E46B0156DDF10BAF8A17437DB66597DEDB6A76D06F5FA2061` respectievelijk `63DD92A1C75132E9361425AC1A79BC489A814EC8751CB41C3DBBD27A93E980CE`); een lokale back-up per host is bewaard. `HaCompanionSensors` en `PawnIO` draaien op beide pc's. De apps zijn na de stille installatie opnieuw gestart in interactieve sessie 1 en 2. Om 13:54 UTC ontving HA op de bestaande CPU-gebruiks- en CPU-temperatuurentities van beide hosts verse numerieke waarden; de 109 entity-identiteiten behielden de bovenstaande vingerafdruk. Daarna is de [publieke desktoprelease 1.0.5](https://github.com/Fill84/HA-Companion-App/releases/tag/1.0.5) vrijgegeven vanaf tagcommit `2b1b521`. De release vermeldt expliciet de ongetekende Windows-binary, het ontbreken van interactieve macOS-/Linux-proeven, de geleverde architecturen en de resterende Windows-randgevalproeven. Die laatste punten zijn niet achteraf als geslaagd aangemerkt.
